# wtx

Play at <https://pakoito.github.io/wtx/>. It installs from the browser menu and plays
offline after the first load.

This repository holds the built site and nothing else. It carries no
history — every deploy replaces the whole tree — and it accepts no
issues or pull requests.
